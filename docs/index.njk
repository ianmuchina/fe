{% set repo = "https://github.com/ianmuchina/fe" %}
{% set branch = "main" %}
{% set img = "design/desktop-preview.jpg" -%}
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Frontend Mentor solutions</title>
    <link rel="stylesheet" href="index.css"/>
  </head>
  <body class="bg-gray-200 py-12 px-4">
    <style>
      @import url("/node_modules/@fontsource/inter/variable.css");
      body {
        font-family: "Inter", "InterVariable", sans-serif;
      }
      .thumbnail:hover > img {
        transform: scale(1.06);
      }
      article {
        display: grid;
        grid-template-rows: 240px auto;
      }
    </style>
    <h1 class="p-6 text-center text-3xl font-bold text-gray-800">
      Frontend Mentor solutions
    </h1>
    <main class="mx-auto grid max-w-5xl gap-2 sm:grid-cols-2 lg:grid-cols-3">
      {% for s in solutions %}
        {% if s.completed %}
          <article
            class="overflow-hidden rounded-md border border-gray-300 bg-white">
            <!-- Card Thumbnail -->
            <div
              class="thumbnail relative overflow-hidden">
              <!-- Thumbnail Image -->
              <img
              src="{{ s.dir }}/{{ img }}" loading="lazy" height="214px" class="translate transition-all duration-300"/>
              <!-- Overlay -->
              <a href="{{ s.dir }}" class="absolute top-0 bottom-0 left-0 right-0 transition-all duration-300 hover:bg-gray-900/20"></a>
            </div>
            <!-- Card Content -->
            <div
              class="flex h-full flex-col justify-between gap-2 p-6">
              {# Title #}
              <h2 class="flex h-full text-xl font-medium hover:underline">
                <a autocapitalize="on" href="{{ s.dir }}">{{ s.name | capitalize }}
                </a>
              </h2>
              <!-- Tags -->
              <a class="flex justify-between">
                <span class="text-sm font-bold">
                  <span class="text-cyan-500">HTML</span>
                  <span class="text-blue-500">CSS</span>
                  {% if s.js %}
                    <span class="text-fuchsia-500">JS</span>
                  {% endif %}
                  {% if s.api %}
                    <span class="text-lime-500">API</span>
                  {% endif %}
                </span>
                {% if s.level == 1 %}
                  <div class="flex overflow-hidden rounded-md border border-cyan-500 text-xs font-bold">
                    <span class="flex items-center bg-cyan-500 px-2 leading-none text-white">{{ s.level }}</span>
                    <span class="flex items-center bg-white px-2 leading-none text-cyan-500">NEWBIE</span>
                  </div>
                {% endif %}
                {% if s.level == 2 %}
                  <div class="flex overflow-hidden rounded-md border border-lime-500 text-xs font-bold">
                    <span class="flex items-center bg-lime-500 px-2 leading-none text-white">{{ s.level }}</span>
                    <span class="flex items-center bg-white px-2 leading-none text-lime-500">JUNIOR</span>
                  </div>
                {% endif %}
              </a>
              <p class="font-medium flex gap-4 ">
                <a class="text-gray-800 decoration-red-500 underline decoration-2" href="{{ repo }}/tree/{{ branch }}/src/{{ s.dir }}">Source</a>
                {# <a class="text-gray-800 decoration-red-500 underline decoration-2" href="{{ s.dir }}">Preview</a> #}
              </p>
            </div>
          </article>
        {% endif %}
      {% endfor %}
    </main>
  </body>
</html>